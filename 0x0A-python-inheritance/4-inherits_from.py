#!/usr/bin/python3
"""Module definininininin lalallaa."""


def inherits_from(obj, a_class):
    """Return what the funciton inherits from."""
    if isinstance(obj, a_class) and type(obj) is not a_class:
        return True
    else:
        return False
