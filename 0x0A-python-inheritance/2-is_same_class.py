#!/usr/bin/python3
"""Somthing something murder something..."""


def is_same_class(obj, a_class):
    """Returns true or false, mysteriously."""
    if type(obj) is a_class:
        return True
    else:
        return False
