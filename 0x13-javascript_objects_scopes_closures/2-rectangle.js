#!/usr/bin/node
/* 2. create empty object if w or h <= 0 */

class Rectangle {
  constructor(w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
}

module.exports = Rectangle
